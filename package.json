{
  "name": "recipe-box",
  "version": "0.1.0",
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-test": "^0.19.1-revision2"
  },
  "devDependencies": {
    "elm-live": "^4.0.2"
  },
  "scripts": {
    "test": "elm-test",
    "build": "elm make src/Main.elm --output dist/elm.js",
    "watch": "elm-live src/Main.elm --dir ./dist --start-page index.html -- --output dist/elm.js"
  }
}